{% extends 'base.html.twig' %}

{% block title %}Alteret index{% endblock %}

{% block content %}
<div class="container mt-4">
    <div class="row">
        <div class="col-lg-12">
            <h2 class="text-center text-primary">Liste des Alterets</h2>
        </div>
    </div>
    <hr/>
    <div class="row">
        <div class="col-lg-12">
            <div class="card shadow-sm">
                <div class="card-header bg-primary text-white">
                    <h4>Gestion des Alterets</h4>
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-striped table-bordered table-hover table-responsive" id="dataTables-example">
                            <thead class="thead-dark">
                                <tr>
                                    <th>Quantité</th>
                                    <th>Prix</th>
                                    <th>Photo</th>
                                    <th>Modèle</th>
                                    <th>Description</th>
                                    <th>Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for Alteret in Alterets %}
                                <tr>
                                    <td>{{ Alteret.Quantite }}</td>
                                    <td>{{ Alteret.Prix }}</td>
                                    <td><img src="{{ asset('uploads/images/' ~ Alteret.photo) }}" alt="Photo de la Alteret" class="img-thumbnail" width="50"></td>
                                    <td>{{ Alteret.Modele }}</td>
                                    <td>{{ Alteret.description }}</td>
                                    <td>
                                        <a href="{{ path('app_Alteret_show', {'id': Alteret.id}) }}" class="btn btn-primary btn-sm">
                                            <i class="fas fa-edit"></i> Modifier
                                        </a>
                                        <a href="{{ path('app_Alteret_edit', {'id': Alteret.id}) }}" class="btn btn-danger btn-sm">
                                            <i class="fas fa-trash-alt"></i> Supprimer
                                        </a>
                                    </td>
                                </tr>
                                {% else %}
                                <tr>
                                    <td colspan="6" class="text-center text-muted">Aucune Alteret trouvée</td>
                                </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>


{% endblock %}
